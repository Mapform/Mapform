import { createSafeActionClient } from "next-safe-action";

/**
 * This doesn't really do anything yet. However, I may want to add some checks
 * here in the future.
 */
export const action = createSafeActionClient();
